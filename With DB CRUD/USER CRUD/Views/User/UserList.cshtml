    @model List<USER_CRUD.Yash.User_Master_Table_1>
@{
    ViewBag.Title = "UserList";
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

<style>
    table, tr, th, td {
        text-align: center;
    }

    th {
        color: black;
    }
</style>
<div>
    <h4>
        @Html.ActionLink("New Create", "User", "User")
    </h4>

    <h2>UserList</h2>
</div>

<br />
<br />


<div class="container-fluid">
    
    <div class="row">
        @using (Html.BeginForm("UserList", "User", FormMethod.Post))
        {
            <div class="form-horizontal">

                <div class="form-group">

                    @*  <label class="col-md-2 control-label">Search</label>

        <div class="col-md-8">
            <input type="text" name="searchtext" class="form-control" placeholder="Search your text hear" style="max-width:100%" />
        </div>

        <div class="col-md-2">
            <button class="btn btn-primary" type="submit">Search</button>
        </div>*@

                    @Html.Label("Search", new { @class = "col-md-2 control-lable" })

                    <div class="col-md-6">
                    
                        @Html.TextBox("searchtext", null, new { @class = "form-control", @placeholder = "Search your text hear", @style = "max-width: 100%" })
                    
                    </div>

                    <div class="col-md-2">
                    
                        <button class="btn btn-primary" type="submit"><span class="glyphicon glyphicon-search"> Search </span></button>
                    
                    </div>


                    <div class="col-md-2">

                        <button type="reset" class="btn btn-danger">Reset</button>
                    
                    </div>

                    </div>
                </div>
        }
    </div>



    <div class="row">
        <div class="col-lg-12">
            <table class="table table-hover table-striped table-bordered">
                <tr>
                    <th>
                        @Html.ActionLink("Name", "UserList", new { SortOrder = ViewBag.SortOrder == null ? "Asc" : (ViewBag.SortOrder == "Asc" ? "Desc" : "Asc"), SortBy = "Name" })
                    </th>

                    <th>
                        @Html.ActionLink("SurName", "UserList", new { SortOrder = ViewBag.SortOrder == null ? "Asc" : (ViewBag.SortOrder == "Asc" ? "Desc" : "Asc"), SortBy = "SurName" })
                    </th>

                    <th>
                        @Html.ActionLink("Email", "UserList", new { SortOrder = ViewBag.SortOrder == null ? "Asc" : (ViewBag.SortOrder == "Asc" ? "Desc" : "Asc"), SortBy = "Email" })
                    </th>

                    <th>
                        @Html.ActionLink("Mobile Number", "UserList", new { SortOrder = ViewBag.SortOrder == null ? "Asc" : (ViewBag.SortOrder == "Asc" ? "Desc" : "Asc"), SortBy = "Mobile Number" })
                    </th>
                    <th>Edit </th>
                    <th>Delete</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.User_MasterFName</td>
                        <td>@item.User_MasterSName</td>
                        <td>@item.User_MasterEmail</td>
                        <td>@item.User_Master_MO_BO</td>
                        <td>
                            @*<a href="@Url.Action("User", new {item.User_MasterID,item.User_MasterFName,item.User_MasterSName,item.User_MasterEmail,item.User_Master_MO_BO })" class="btn btn-success">Edit</a>*@
                            <a href="@Url.Action("Edit",new {@id = item.User_MasterID})" class="btn btn-success">Edit</a>
                        </td>
                        @*<td>
                            @Html.ActionLink("Edit", "Edit", new {id = item.User_MasterID}, new { @class="btn btn-success"})
                        </td>*@
                        <td>
                            <a href="@Url.Action("Delete",new {@id = item.User_MasterID})" class="btn btn-danger"  onclick="return confirm('Do you really want this recode?')">Delete</a>
                        </td>
                         @*<td>
                            @Html.ActionLink("Delete", "Delete", new {id = item.User_MasterID}, new { @class= "btn btn-danger", onclick="return confirm('Do you really want this recode?')" })
                        </td>*@
                    </tr>
                }
            </table>
        </div>
    </div>
</div>

@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")

